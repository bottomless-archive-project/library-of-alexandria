dependencies {
    // TODO: This shouldn't be here! It's only here because of com.github.bottomlessarchive.loa.vault.service.archive.ArchivingService.isDuplicateIndexError.
    // However, instead of checking that there, we should catch it in a lower layer. and throw our own exception there!
    implementation group: 'org.mongodb', name: 'mongodb-driver-reactivestreams'

    implementation group: 'commons-io', name: 'commons-io', version: '2.11.0'
    implementation group: 'software.amazon.awssdk', name: 's3', version: '2.17.257'

    implementation project(':loa-library:loa-document-service')
    implementation project(':loa-library:loa-staging-client-service')

    testImplementation group: 'com.google.jimfs', name: 'jimfs', version: '1.2'
}
